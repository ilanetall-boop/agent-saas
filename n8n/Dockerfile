# N8N Docker Image for Render
FROM n8nio/n8n:latest

# Environment variables
ENV N8N_PORT=5678
ENV N8N_PROTOCOL=https

# Expose port
EXPOSE 5678

# Start N8N (the image already has the entrypoint configured)
